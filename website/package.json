{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build --fail-on-error",
    "dev": "nuxt dev --fail-on-error",
    "generate": "nuxt generate --fail-on-error",
    "preview": "nuxt preview --fail-on-error",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.7.0",
    "@types/node": "^18",
    "nuxt": "^3.5.0"
  },
  "dependencies": {
    "npm": "^10.4.0"
  }
}
